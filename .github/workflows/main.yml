name: auto-build

on:
  schedule:
    - cron: '0 0 * * *'

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Trigger Netlify build
      run: curl -X POST -d '{}' https://api.netlify.com/build_hooks/5e588b5fc4f099f558db3f05
