[build]
  command = """
  pip3 install -q poetry &&
  poetry config virtualenvs.in-project true &&
  poetry install -v &&
  poetry run mkdocs build -d site
  """
  publish = "site"

[build.environment]
  PYTHON_VERSION = "3.12"
